/** @type {import('next').NextConfig} */
const nextConfig = {
  // Frames are served from /public/ezgif-split/ as static assets.
  // No Next/Image optimisation needed — we draw them directly to Canvas.
};

export default nextConfig;
