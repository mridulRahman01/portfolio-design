# ── Supabase ───────────────────────────────────────────────────
# Project: https://supabase.com/dashboard/project/jfkefdwxeschxclbcivo
SUPABASE_URL="https://jfkefdwxeschxclbcivo.supabase.co"
# Project Settings → API keys → service_role (secret). Server-side only —
# never expose this in client code or commit it.
SUPABASE_SERVICE_ROLE_KEY=""

# ── Auth (NextAuth) ────────────────────────────────────────────
# Generate: openssl rand -base64 32
NEXTAUTH_SECRET="replace-with-a-long-random-string"
NEXTAUTH_URL="http://localhost:3000"

# ── Cloudinary (Media Library) ─────────────────────────────────
# Dashboard → Settings → API Keys
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""

# ── Content scheduler (Vercel Cron auth) ───────────────────────
CRON_SECRET="replace-with-another-random-string"

# ── Optional: lead email notifications via Resend ──────────────
RESEND_API_KEY=""
LEAD_NOTIFY_EMAIL=""
